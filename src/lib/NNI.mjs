export default class KNNI {

    constructor(imageData){
        this.imageData = imageData;
    };

    /**
     * 
     * @param {data} data 
     * @returns {ImageData} ImageData
     */
    NearestNeighborsInterpolation(){

        let data = this.imageData.data;
        
        /**
         * implementation of the algo..
         */
    };
};